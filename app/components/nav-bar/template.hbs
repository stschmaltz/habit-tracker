<div local-class="nav-bar__container" id="main-navbar">
  <h2 local-class="nav-bar__title">Habit Tracker</h2>
  <div local-class="nav-bar__icon-container">

    {{fa-icon "plus" local-class="nav-bar__plus" click=(action "toggleModal")}}
    {{fa-icon "bars" local-class="nav-bar__hamburger"}}
    {{#if isShowingModal}}
    {{#add-new-habit-dialog
      onClose="toggleModal"
      targetAttachment="center"
      translucentOverlay=true
  }}
    Hi there!
    {{/add-new-habit-dialog}}
    {{/if}}
  </div>
</div>
<div local-class="nav-bar__underneath"></div>